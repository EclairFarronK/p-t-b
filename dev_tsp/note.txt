export
    pip freeze > requirements.txt

pip install python-telegram-bot[socks]
    直接这样就不需要先pip install python-telegram-bot
pip install telethon
pip install python-socks[asyncio]
pip install pyyaml
pip install pymongo

todo
    熟悉message的格式
    保存的数据结构
    client
        1,接入MongoDB，存储消息的格式应该是怎么样的？高效的数据结构，考虑全局搜索。
        2,加入某个群将群内所有消息保存在MongoDB，先做一次全量同步，如果有新的消息，将新的消息存档。
        3,可以根据条件来判定是否需要同步群内消息？如果是搜索qun就不需要同步消息（怎么判定搜索群？）
        4,可以直接购买一个加入了很多channel和group的账号，直接使用账号来同步
    bot
        1，加入group/channel后，将所有消息都保存下来。
            需要邀请才能加入吗？
            消息更新时，可以同步更新
        2，u-b1-b2，将返回的消息保存
            同一条消息不变，但是内容变化了，这个是怎么实现的？
        3，做一个搜索bot
    MongoDB
        1，设置保存数据的格式
        2，在服务器上搭建MongoDB，方便导入导出
        3，怎么方便快速搜索？


















todo
    除了频道之后的消息自己保存
    保存group，channel
    频道中的消息，直接跳过去