data source：
    insert：
        1,通过dialog导入
            批量操作、除了名字其他所有都要更新、没有就插入，有就更新
            update_one(filter=filter, update=update, upsert=True)
            {'title': title,'username': username,'type': type,'create_time': dt,'update_time': dt,'state': 1}
        2,从其他bot爬取
            批量操作、没有就插入，有就更新
            update_one(filter=filter, update=update, upsert=True)
        3,通过文档导入
            trait：数据可能不是最新，只insert不update
            {'username': username,'create_time': dt,'update_time': dt,'state': 1}
            insert_many(data, ordered=False)
    update：

    todo
        state在什么时候使用？
        telethon的日志记录功能